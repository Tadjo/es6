// library.js
define(['lodash', 'moment'], function(_, moment) {
    function sortByDate(date, order) {};
    function groupByDate(date) {};
    return {
        sortByDate,
        groupByDate,
    }
});
